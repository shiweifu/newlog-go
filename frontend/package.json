{
  "name": "newlog_frontend",
  "scripts": {
    "build": "npx parcel build --target css --target js --no-cache",
    "watch": "npx parcel watch --target css --target js --no-cache"
  },
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@parcel/transformer-sass": "2.8.0",
    "autoprefixer": "^10.4.13",
    "cssnano": "^5.1.14",
    "parcel": "^2.8.0",
    "postcss": "^8.4.19",
    "postcss-import": "^15.0.0",
    "tailwindcss": "^3.2.4"
  },
  "targets": {
    "js": {
      "source": "./application.js",
      "distDir": "../assets/js"
    },
    "css": {
      "source": "./application.scss",
      "distDir": "../assets/css"
    }
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.8",
    "github-markdown-css": "^5.1.0"
  }
}
